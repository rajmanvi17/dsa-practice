https://leetcode.com/problems/reverse-nodes-in-k-group/submissions/2010105913

Pattern- Reversal

Code-

var reverseKGroup = function(head, k) {

    let dummy = new ListNode(0);
    dummy.next = head;

    let groupPrev = dummy;

    while (true) {

        // find kth node
        let kth = groupPrev;

        for (let i = 0; i < k; i++) {
            kth = kth.next;

            if (!kth) {
                return dummy.next;
            }
        }

        let groupNext = kth.next;

        // reverse group
        let prev = groupNext;
        let curr = groupPrev.next;

        while (curr !== groupNext) {
            let temp = curr.next;

            curr.next = prev;
            prev = curr;
            curr = temp;
        }

        // reconnect
        let temp = groupPrev.next;

        groupPrev.next = kth;
        groupPrev = temp;
    }
};
