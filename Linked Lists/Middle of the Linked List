https://leetcode.com/problems/middle-of-the-linked-list/submissions/2012906032

Pattern- Fast & Slow

Code-

var middleNode = function(head) {
    let slow = head;
    let fast = head;

    while (fast && fast.next) {
        slow = slow.next;
        fast = fast.next.next;
    }

    return slow;
};
