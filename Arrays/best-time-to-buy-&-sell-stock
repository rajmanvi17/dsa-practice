var maxProfit = function(prices) {

    let minPrice = prices[0];  //step 1
    let maxProfit = 0;        //step 2

    for(let i = 0;  i < prices.length; i++) {   //step 3

        if(prices[i] < minPrice) {              //step-4
            minPrice = prices[i];
        } else {
            let profit = prices[i] - minPrice;

            if(profit > maxProfit) {
                maxProfit = profit;
            }
        }
    }
